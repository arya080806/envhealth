# 项目 Git 保存记录

记录时间：2026-06-11 15:15:07 +08:00  
项目目录：`D:\SSH\环境游戏程序\healing-environment`  
当前分支：`master`

## 本次项目快照

| 项目 | 内容 |
| --- | --- |
| 保存提交短号 | `dc64579` |
| 保存提交完整哈希 | `dc645795afa99d3f15eb84ee12c3301b4aba6a01` |
| 提交时间 | `2026-06-11T15:14:54+08:00` |
| 提交人 | `Codex <codex@local>` |
| 提交说明 | `保存当前项目快照` |

## 保存范围

本次快照提交保存了当前项目工作区的代码、文档、静态资源、设计参考、测试报告、备份源码和工具脚本等内容。

提交统计：

- 变更文件：152 个
- 新增行：30104 行
- 删除行：2890 行

未保存到 Git 的运行时/敏感内容按 `.gitignore` 规则排除，包括：

- `.env`
- `data/`
- `uploads/`
- `outputs/`
- `*.db`
- `server*.log`
- `.venv/`
- `.nicegui/`

## 备注

本文件用于记录项目快照提交信息。由于 Git 提交哈希由提交内容计算而来，记录文件本身会作为后续单独提交保存。

---

## 2026-06-13 envhealth 结果页与飞书同步修复

| 项目 | 内容 |
| --- | --- |
| 修复提交短号 | `d0e6315` |
| 修复提交完整哈希 | `d0e631560b2cf0704f23d5bb66b8b9a5775fa91e` |
| 提交时间 | `2026-06-13T12:26:29+08:00` |
| 提交人 | `Codex <codex@local>` |
| 提交说明 | `修复结果页图片展示与飞书同步字段` |
| 修复报告 | `docs/20260613_envhealth_result_feishu_fix_report.md` |

本次修复内容：

- 统一结果页大图对比模块：默认显示改造后大图，滑杆查看原图，右上角可查看原图，长按可下载改造后图片。
- 提升结果图和画布快照展示清晰度，新增图片变体服务并在图片保存时预热展示图/缩略图。
- 修正画布历史来源，避免最终生成图误显示为元素布局图。
- 扩展自由创作、灵感创想、对话改造、智能参数的飞书同步字段。
- 完成本地编译、图片接口、下载接口和飞书字段映射校验。

### 追加修复：飞书失效记录重建

| 项目 | 内容 |
| --- | --- |
| 修复提交短号 | `0a63f3c` |
| 修复提交完整哈希 | `0a63f3c42cda45d6b0674c3904cdb899fa8dbbba` |
| 提交时间 | `2026-06-13T12:33:33+08:00` |
| 提交人 | `Codex <codex@local>` |
| 提交说明 | `修复飞书失效记录重建` |

线上同步重试发现 `work_count_summary` 的旧 `remote_record_id` 在飞书端已不存在。本次追加补丁让飞书 upsert 在遇到 `record not found` 时自动插入新记录，避免失败队列长期卡住。

### 追加修复：飞书批量更新失效记录重建

| 项目 | 内容 |
| --- | --- |
| 修复提交短号 | `ca069d2` |
| 修复提交完整哈希 | `ca069d2253ee847c873be9ae1311f8f7b69d524a` |
| 提交时间 | `2026-06-13T12:36:32+08:00` |
| 提交人 | `Codex <codex@local>` |
| 提交说明 | `修复飞书批量更新失效记录重建` |

前一个补丁覆盖了单条 upsert，同步重试继续发现批量更新路径也可能遇到同类失效 record_id。本次补齐批量更新兜底：批量更新失败且错误为 record not found 时，自动逐条重新 upsert。

### 线上配置补记

2026-06-13 12:37-12:39 已在服务器 `/home/zm/envhealth/.env` 完成飞书表 ID 补充：

- `FEISHU_BITABLE_CHAT_SUMMARY_TABLE_ID=tblZkSIqni3IjZ9J`
- `FEISHU_BITABLE_SLIDER_SUMMARY_TABLE_ID=tbl0b7OU7sommUqK`
- `FEISHU_BITABLE_SESSIONS_TABLE_ID=tblJ8uNrq1VbskhJ`

已重启 `envhealth.service`，健康接口正常。随后执行飞书同步重试，最终队列状态为 `success=23`、`failed=0`、`latest_failures=[]`。

---

## 2026-06-16 身份审计修复与画布优化快照

记录时间：2026-06-16 23:05:12 +08:00  
项目目录：`D:\SSH\环境游戏程序\healing-environment`  
当前分支：`master`（上游：`origin/main`）

| 项目 | 内容 |
| --- | --- |
| 保存提交短号 | `1cfd563` |
| 保存提交完整哈希 | `1cfd563666e822ac74b2bbb24e418acb36b3cc7e` |
| 提交时间 | `2026-06-16T23:04:57+08:00` |
| 提交人 | `Codex <codex@local>` |
| 提交说明 | `保存身份审计修复与画布优化快照` |

本次保存内容：

- 新增身份数据审计、修复和残留清理工具脚本，并保存多轮审计/修复结果文档。
- 优化研究登录匹配逻辑，识别重复参与者编号和重复账号，降低误登录风险。
- 优化生成完成通知查询与已读标记，改为数据库侧过滤和批量更新。
- 增强拖拽画布保存失败提示、异常日志和画布元素顺序持久化。
- 修正结果页画布历史删除后的网格节点移除和重排。
- 增加飞书唯一字段查找时的重复记录检测，避免静默写入错误远端记录。

提交统计：

- 变更文件：25 个
- 新增行：11710 行
- 删除行：66 行

校验记录：

- `py -3 -m compileall app tools`
- `uv run python -m compileall app tools`
