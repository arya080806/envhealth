[project]
name = "healing-environment"
version = "0.1.0"
description = "环境疗愈生成程序 - 基于环境心理学的数字化干预工具"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "nicegui>=3.9.0",
    "pillow>=12.1.1",
]
